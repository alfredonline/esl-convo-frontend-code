import styled from "@emotion/styled";

const DefinitionHolder = styled("Typography")(() => ({
  backgroundColor: "#37ca90",
  color: "#fff",
  fontSize: "18px",
  fontWeight: "600",
  borderRadius: "12px",
  padding:"12px"
}));

export default DefinitionHolder;
