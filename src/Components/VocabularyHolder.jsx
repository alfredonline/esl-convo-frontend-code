import styled from "@emotion/styled";

const VocabularyHolder = styled("Typography")(() => ({
  backgroundColor: "#6494ff",
  color: "#fff",
  fontSize: "18px",
  fontWeight: "600",
  padding: "12px",
  borderRadius: "12px"
}));

export default VocabularyHolder;
