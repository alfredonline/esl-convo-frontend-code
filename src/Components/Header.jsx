import styled from "@emotion/styled";

const Header = styled("Typography")(() => ({
  fontWeight: "700",
  fontSize: "42px",
  color: "#2c343e",
  paddingBottom: "30px",
  paddingTop: "30px"
}));

export default Header;
